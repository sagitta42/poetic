api:
  image: python:3.12-slim
  working_dir: /app
  environment:
    APP_NAME: "REPLACEME"
  volumes:
    - :/app
  command: sh -c "poetry install && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  depends_on:
    db:
      condition: service_healthy
  ports:
    - '8000:8000'
