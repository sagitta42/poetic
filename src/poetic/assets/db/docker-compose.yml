services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_URL}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - 5432:5432
    working_dir: /db
    volumes:
      - .:/db